1.Promise的基本特性：
    1.对象的状态不受外界影响
    2.一旦状态改变，就不可以再变，什么时候访问都是这个结果
    3.promise不能够取消执行
2.从语法上解释：
    Promise是一个对象，接受一个方法，有两个参数，分别代表成功，失败。
    Promise的第一个then方法是成功之后执行，第二个catch是失败之后执行
3.then和catch总是在代码执行完毕之后再执行，因为js引擎对于事件处理的逻辑
